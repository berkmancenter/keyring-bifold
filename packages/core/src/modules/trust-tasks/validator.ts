/**
 * The payload validator the §7.2 pipeline requires — implementation in
 * @bifold/trust-tasks (shared with the witness-server); re-exported
 * here so the wallet's import paths stay stable.
 */
export { trustTaskPayloadValidator } from '@bifold/trust-tasks'
