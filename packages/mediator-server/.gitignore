# Askar store — per-developer, regenerated by `yarn fresh` / a first boot.
.wallet/
# Written on boot for the spin-up harness to read; carries a live endpoint.
# `-v2` is the DIDComm v2 invitation the same boot writes in v2 mode.
.mediator-invitation
.mediator-invitation-v2
dist/
